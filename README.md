# 5_project — Minimal Flask app

This folder contains a minimal Flask web app.

Quick start (PowerShell):

```powershell
# optional: create virtual environment
python -m venv .venv
; .\.venv\Scripts\Activate.ps1
; python -m pip install --upgrade pip
; pip install -r requirements.txt
; python app.py
```

Open http://localhost:5000 in your browser. API: `http://localhost:5000/api/hello`.
